import reverseString from "./reverse_string.js";
// write at least one test function
describe('reverseString', () => {
  it('should return the string reversed', () => {
    expect(reverseString('string')).toBe('gnirts');
  }
  );
});